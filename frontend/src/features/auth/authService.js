// Placeholder auth service
export const login = async (credentials) => {
  // TODO: implementar llamada real a la API
  return { token: 'fake-token', user: { id: 1, name: 'Demo User' } };
};

export const register = async (data) => {
  // TODO: implementar llamada real a la API
  return { success: true };
};
